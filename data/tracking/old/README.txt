Date: 1683774466.1028779

Data collected before the date above was collected before the batch (batch number) column was added.

Data after the date above includes a new catch column. This column serves as a batch indicator indicating what batch is written to the dataset.